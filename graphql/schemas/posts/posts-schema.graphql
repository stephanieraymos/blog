type Query {
  trendingPosts: [Post] ! 
  featuredPosts: [Post] ! 
  recentPosts: [Post] ! 
}

type Post {
  id: Int,
  title: String
  date: String
  catergories: [String]
  author: String
  description: String
  image: String
}
